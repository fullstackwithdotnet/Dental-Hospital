@using Metron.Entities
@model DIMS.ViewModels.OMRViewModel
@functions{
    public string GetClassFromProperties(string propertyName)
    {
        var props = Model.Proplist.FirstOrDefault<OMRCasesheetProperties>(x => x.PropertyName == propertyName);

        if (props != null && props.PropertyVisibility == "N")
        {
            return "display:none";
        }

        return "";
    }
}
<div class="row">
    <div class="col-md-12">

        <div class="row">
            <div class="form-group form-group-sm">
                @Html.LabelFor(x => x.AreaRadiographed, new { @class = "col-md-2 control-label" })
                <div class="col-md-9">
                    @Html.TextAreaFor(x => x.AreaRadiographed, new { @class = "form-control" })
                </div>
            </div>
        </div>

        

        <div class="row">
            <div class="form-group form-group-sm">
                @Html.LabelFor(x => x.Root, new { @class = "col-md-2 control-label" })
                <div class="col-md-9">
                    @Html.TextAreaFor(x => x.Root, new { @class = "form-control" })
                </div>
            </div>
        </div>

        <div class="row">
            <div class="form-group form-group-sm">
                @Html.LabelFor(x => x.NormalLandMarks, new { @class = "col-md-2 control-label" })
                <div class="col-md-9">
                    @Html.TextAreaFor(x => x.NormalLandMarks, new { @class = "form-control" })
                </div>
            </div>
        </div>

        <div class="row">
            <div class="form-group form-group-sm">
                @Html.LabelFor(x => x.RadiographicFault, new { @class = "col-md-2 control-label" })
                <div class="col-md-9">
                    @Html.TextAreaFor(x => x.RadiographicFault, new { @class = "form-control" })
                </div>
            </div>
        </div>

        <div class="row">
            <div class="form-group form-group-sm">
                @Html.LabelFor(x => x.RadiographicDiagnosis, new { @class = "col-md-2 control-label" })
                <div class="col-md-9">
                    @Html.TextAreaFor(x => x.RadiographicDiagnosis, new { @class = "form-control" })
                </div>
            </div>
        </div>

        <div class="row">
            <div class="form-group form-group-sm">
                @Html.LabelFor(x => x.Grade, new { @class = "col-md-2 control-label" })
                <div class="col-md-9">
                    @Html.TextAreaFor(x => x.Grade, new { @class = "form-control" })
                </div>
            </div>
        </div>
    </div>
</div>


